<?xml version="1.0" encoding="utf-8"?>
<!--
  NDEF Type-4 tag emulation. Registers the NFC Forum "NDEF Tag Application" AID so
  a plain NFC read (the other phone does not need Tapio in the foreground) sees an
  NDEF message and Android dispatches it to Tapio via its NDEF_DISCOVERED filter.
-->
<host-apdu-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/tapio_ndef_service_description"
    android:requireDeviceUnlock="false">

    <aid-group
        android:description="@string/tapio_ndef_aid_group_description"
        android:category="other">
        <aid-filter android:name="D2760000850101" />
    </aid-group>
</host-apdu-service>
